Sub Debug_ShowContentControlTypes()
    Dim cc As ContentControl
    Dim msg As String

    msg = "Found the following content controls:" & vbCrLf & vbCrLf

    For Each cc In ActiveDocument.ContentControls
        msg = msg & "Type ID: " & cc.Type & " | Title: " & cc.Title & " | Tag: " & cc.Tag & vbCrLf
    Next cc

    If msg = "Found the following content controls:" & vbCrLf & vbCrLf Then
        MsgBox "No content controls found!", vbExclamation, "Debug Info"
    Else
        MsgBox msg, vbInformation, "Content Controls Type IDs"
    End If
End Sub
