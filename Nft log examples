Sub GetContentControlCheckboxInfo()
    Dim cc As ContentControl
    
    ' Перебираем все элементы управления содержимым
    For Each cc In ActiveDocument.ContentControls
        ' Проверяем, является ли элемент чекбоксом
        If cc.Type = wdContentControlCheckBox Then
            ' Выводим информацию о чекбоксе в окно отладки
            Debug.Print "Title: " & cc.Title
            Debug.Print "Tag: " & cc.Tag
            Debug.Print "Checked: " & cc.Checked
            Debug.Print "ID: " & cc.ID
            Debug.Print "Range Text: " & cc.Range.Text
            Debug.Print "----------------------------"
        End If
    Next cc
End Sub
