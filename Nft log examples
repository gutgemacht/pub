Sub UncheckAllCheckboxes()
    Dim chk As ContentControl
    For Each chk In ActiveDocument.ContentControls
        If chk.Type = wdContentControlCheckBox Then
            chk.Checked = False
        End If
    Next chk
End Sub
