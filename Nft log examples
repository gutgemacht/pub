Sub ChangeAllCheckBoxes()
    Dim cc As ContentControl
    Dim newState As Long
    
    ' Set the state you want to select
    ' 1 = Checked
    ' 2 = Unchecked
    ' 3 = Not defined
    newState = 1 ' Change to the desired state
    
    ' Loop through all ContentControl objects in the document
    For Each cc In ActiveDocument.ContentControls
        If cc.Type = wdContentControlCheckBox Then
            ' Check if the title or tag matches
            If cc.Title = "Check" Or cc.Tag = "Check" Then
                ' Change the state based on newState
                Select Case newState
                    Case 1 ' Checked
                        cc.Range.FormFields(1).CheckBox.Value = True
                    Case 2 ' Unchecked
                        cc.Range.FormFields(1).CheckBox.Value = False
                    Case 3 ' Not defined (if needed, use default or leave empty)
                        cc.Range.FormFields(1).CheckBox.Value = wdUndefined
                End Select
            End If
        End If
    Next cc
End Sub
